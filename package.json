{
  "name": "lambda-demo-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node ./index.js",
    "build": "rm -f dist.zip && zip -r dist.zip .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "David BALAN",
  "license": "ISC",
  "dependencies": {
    "aws-serverless-express": "^3.1.3",
    "express": "^4.16.3",
    "huh": "^2.0.1",
    "superb": "^2.0.0"
  }
}
